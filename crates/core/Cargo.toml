[package]
name = "aistatus-core"
version = "0.1.0"
edition.workspace = true

[lib]
name = "aistatus_core"

[features]
default = ["live"]
live = []
fixture-only = []

[dependencies]
serde.workspace = true
serde_json.workspace = true
