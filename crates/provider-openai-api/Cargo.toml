[package]
name = "aistatus-provider-openai-api"
version = "0.1.0"
edition.workspace = true

[lib]
name = "aistatus_provider_openai_api"

[dependencies]
aistatus_core = { package = "aistatus-core", path = "../core", default-features = false }
